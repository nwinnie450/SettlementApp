# MongoDB Connection
# For production/local machine use MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority&appName=settlement

# For development with network restrictions (uses in-memory MongoDB)
USE_MEMORY_DB=true

# Or use local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/settlementapp

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-12345

# Server Configuration
PORT=5000
NODE_ENV=development

# Cloudinary Configuration (optional - for photo uploads)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
